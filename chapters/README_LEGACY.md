# Legacy Chapter Mirror

This directory is maintained for backward compatibility with older local workflows.

- Canonical chapter sources are in `gitbook/chapters/`.
- Canonical front matter is in `gitbook/front_matter/`.
- Prefer editing GitBook sources directly.

If you need this directory for a legacy build pipeline, regenerate or sync it from `gitbook/` content rather than authoring here.
